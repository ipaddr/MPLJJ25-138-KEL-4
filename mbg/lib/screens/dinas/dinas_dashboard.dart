import 'package:flutter/material.dart';

class DinasDashboard extends StatelessWidget {
  const DinasDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Dashboard Dinas Pendidikan"),
    );
  }
}