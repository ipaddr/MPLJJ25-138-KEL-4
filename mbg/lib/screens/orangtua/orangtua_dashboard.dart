import 'package:flutter/material.dart';

class OrangTuaDashboard extends StatelessWidget {
  const OrangTuaDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Dashboard Orang Tua"),
    );
  }
}