import 'package:flutter/material.dart';

class KateringDashboard extends StatelessWidget {
  const KateringDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Dashboard Tim Katering"),
    );
  }
}
